{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "SW Song 포트폴리오 진입 페이지 (Next.js, 정적 export)",
  "scripts": {
    "build": "node scripts/generate-posts.mjs && next build",
    "dev": "node scripts/generate-posts.mjs && next dev",
    "dev:lan": "node scripts/generate-posts.mjs && next dev --hostname 0.0.0.0 --port 3000",
    "start": "next start",
    "serve:static": "node scripts/serve-static.mjs",
    "lint": "eslint"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "16.2.9",
    "next-themes": "^0.4.6",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-markdown": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "typescript": "^5"
  }
}
